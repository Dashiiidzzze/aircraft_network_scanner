# Базовый минимальный образ Python
FROM python:3.11-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем скрипт в рабочую директорию контейнера
COPY . .

# Запуск основного скрипта при старте контейнера
CMD ["python", "sensors.py"]
